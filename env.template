# 마케팅 캠페인 관리 시스템 - 환경변수 설정
# ================================================

# 🔒 보안 관련 설정
# ==================
# JWT 토큰 서명용 비밀키 (256비트 이상 권장)
JWT_SECRET=your-super-secret-jwt-key-here-minimum-32-characters

# 세션 만료 시간 (초 단위, 기본: 24시간)
JWT_EXPIRES_IN=86400

# 비밀번호 해시 라운드 (기본: 12, 높을수록 보안 강화되지만 성능 저하)
BCRYPT_ROUNDS=12

# 🗄️ 데이터베이스 설정
# =====================
# MySQL 연결 정보
DB_HOST=localhost
DB_PORT=3306
DB_USER=campaign_user
DB_PASSWORD=your-secure-database-password
DB_NAME=campaign_db

# 연결 풀 설정
DB_CONNECTION_LIMIT=10
DB_QUEUE_LIMIT=0
DB_TIMEOUT=60000

# 📧 이메일 설정 (선택사항)
# ===========================
# SMTP 서버 설정
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@company.com
SMTP_PASSWORD=your-app-password

# 발송자 정보
EMAIL_FROM_NAME=Campaign System
EMAIL_FROM_ADDRESS=no-reply@company.com

# 🌐 애플리케이션 설정
# =====================
# 운영 환경 설정
NODE_ENV=production
PORT=3000
HOSTNAME=0.0.0.0

# 애플리케이션 URL (CORS 및 알림에서 사용)
APP_URL=https://your-domain.com

# 🔐 추가 보안 설정
# ==================
# CORS 허용 도메인 (쉼표로 구분)
ALLOWED_ORIGINS=https://your-domain.com,https://admin.your-domain.com

# API Rate Limiting (분당 요청 수)
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW_MS=60000

# 🗂️ 파일 업로드 설정
# =====================
# 업로드 디렉토리
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=10485760

# 📊 로깅 설정
# =============
# 로그 레벨 (error, warn, info, debug)
LOG_LEVEL=info

# 로그 파일 경로
LOG_FILE_PATH=./logs

# 로그 파일 보관 기간 (일)
LOG_RETENTION_DAYS=30

# 📈 모니터링 설정
# =================
# 애플리케이션 메트릭 수집 여부
ENABLE_METRICS=true

# 헬스체크 엔드포인트 활성화
ENABLE_HEALTH_CHECK=true

# 🏢 회사 정보
# =============
COMPANY_NAME=Your Company Name
COMPANY_CONTACT=support@company.com
SYSTEM_ADMIN_EMAIL=admin@company.com

# 🎨 UI 설정
# ===========
# 애플리케이션 제목
APP_TITLE=마케팅 캠페인 관리 시스템
APP_DESCRIPTION=통합 마케팅 캠페인 관리 및 분석 플랫폼

# 기본 언어
DEFAULT_LANGUAGE=ko
DEFAULT_TIMEZONE=Asia/Seoul

# 📋 기능 플래그
# ===============
# 기능별 활성화/비활성화
ENABLE_REGISTRATION=false
ENABLE_PASSWORD_RESET=true
ENABLE_EMAIL_NOTIFICATIONS=true
ENABLE_EXPORT_FEATURES=true
ENABLE_ANALYTICS_DASHBOARD=true

# 🔧 개발/디버그 설정 (운영환경에서는 false로 설정)
# ====================================================
DEBUG_MODE=false
ENABLE_SQL_LOGGING=false
ENABLE_DETAILED_ERRORS=false 